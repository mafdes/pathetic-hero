const fs = require('fs');
const path = require('path');
const { createCanvas } = require('canvas'); // Let's check if canvas module works, or use raw PNG / Phaser

// Si no está el módulo canvas de node, podemos generarlo mediante un HTML/Phaser script o con pngjs / script sencillo.
